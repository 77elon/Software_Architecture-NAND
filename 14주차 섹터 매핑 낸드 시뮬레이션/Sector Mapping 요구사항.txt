
이후 소프트웨어 공학 섹터맵핑, 블록맵핑 보고서 작성 후 코딩
디지털논리설계 강의 + 실험실습
자바실무 퀴즈 + 강의

input -> read write (From File System)뿐

write -> override시 예외처리
이는 Sector Mapping, Block Mapping시 모두 다르게 구현해야 함
평소에는 빈 섹터에 저장하다가 섹터가 꽉차면 여분 블록에 저장한다.
if) 여분 Block이 꽉찼다면? <- 그냥 Block 전체 Indexing 해서 데이터가 들어있는지 확인하자
실질적으로 사용하는 데이터의 구분? MappingTable에서 PSN 색인
해당 블록에서 사용하는 데이터만 다른 빈 섹터에 저장하고, MappingTable 수정 후  여유 블록을 지운다. (여유 블록 이외에 저장 필요 x)
여기서 문제가 역순으로 배정되어 있는 Table 내에서 어딘가에 매핑되어있을 것이기에, 여유 블록 삭제 후 여유 블록에 매핑, 이 때 실제로 사용하지 않는 데이터의 추적은?
블록매핑에서는 블록 자체를 바꿔도 될 듯 하다.

Mapping table(Vector Type)은 RAM에 있으니, 색인에 제한이 없다!
해당 Block에 Mapping 되어 있는 Sector 확인 후 그 데이터들만 백업한다음에 블록을 비운다. (어차피 마지막 블록을 여분으로 할 것 이기에)

기본적으로 정적으로 테이블 할당, override시 table 할당 번호만 변경
but, 처음 테이블 할당시에만 역순임을 인지하고 기능 구현!
역순을 유지한다면 FTL 존재 의미 무의미
if block is full? 

항상 쓰기 시, 테이블 저장!

테이블 내에 erase시 올라가는 수명 정보
사용중인 메모리 용량
매핑 정보

섹터맵핑 섹터가 꽉차있다면 희생블록에 데이터 저장, mapping, 그리고 유휴상태에서 희생블록에 데이터가 있다면, 같이 기록된 LSN을 기반으로 